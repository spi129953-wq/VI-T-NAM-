name: SSH via fxTunnel

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest

    steps:
      - name: Install SSH server
        run: |
          sudo apt update
          sudo apt install -y openssh-server curl
          sudo mkdir -p /var/run/sshd

      - name: Configure SSH
        run: |
          sudo useradd -m runner || true
          echo "runner:123456" | sudo chpasswd

          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/g' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/g' /etc/ssh/sshd_config

          sudo systemctl enable ssh
          sudo systemctl restart ssh

      - name: Install fxTunnel
        run: |
          curl -L https://github.com/FXTunnels/fxtunnel/releases/latest/download/fxtunnel-linux-amd64 -o fxtunnel
          chmod +x fxtunnel

      - name: Start fxTunnel
        run: |
          ./fxtunnel tcp 22 > fxtunnel.log 2>&1 &
          sleep 15

      - name: Extract SSH endpoint
        run: |
          ENDPOINT=$(grep -oE 'tcp://[^ ]+' fxtunnel.log | head -n1 | sed 's/tcp:\/\///')
          HOST=$(echo $ENDPOINT | cut -d: -f1)
          PORT=$(echo $ENDPOINT | cut -d: -f2)

          echo ""
          echo "=============================="
          echo "SSH USER: runner"
          echo "SSH PASS: 123456"
          echo ""
          echo "SSH COMMAND:"
          echo "ssh runner@$HOST -p $PORT"
          echo "=============================="
          echo ""

          echo "::notice::SSH => ssh runner@$HOST -p $PORT"

      - name: Show fxTunnel log
        run: |
          echo "========== FXTUNNEL LOG =========="
          cat fxtunnel.log
          echo "=================================="

      - name: Keep runner alive
        run: |
          echo "Runner alive for ~6 hours"
          sleep 21600
